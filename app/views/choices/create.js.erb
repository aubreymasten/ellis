// add choice to choice list in edit panel
// add re-render scene card
$("#scene-choices")
.empty()
.append("<%= j render partial: 'scenes/choice_list', locals: { scene: @scene }%>");
$("#scene-<%= @scene.id%>")
.after("<%= j render partial: 'scenes/card', locals: { scene: @scene }%>")
.remove();
